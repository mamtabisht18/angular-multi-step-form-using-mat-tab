import { CardSeperatorPipe } from './card-seperator.pipe';

describe('CardSeperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CardSeperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
